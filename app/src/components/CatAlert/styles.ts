import {StyleSheet} from 'react-native';


export const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
  },
  cat: {
    fontSize: 50,
    textAlign: 'center',
    color: 'gray',
    marginBottom: 10,
  },
  title: {
    textAlign: 'center',
    alignSelf: 'center',
    maxWidth: 200,
    color: 'gray',
  },
});
